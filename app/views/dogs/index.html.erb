<h1> Dog Index Page </h1>
    <ul>
        <%@dogs.each do |dog| %>
            <li><%= link_to dog.name, dog_path(dog) %></li>
            <% end %>
    </ul>
    <div><%= link_to "add_dog", new_dog_path %> </div>

    