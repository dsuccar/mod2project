<%= form_for @app do |f|%>
<%= f.label "Select A User" %>
<%= f.collection_select(:user_id, User.all, :id, :name) %>
<%= f.label "Select A Dog" %>
<%= f.collection_select(:dog_id, Dog.non_adopted, :id, :name) %>
<%= f.submit "Adopt" %>
<% end%>

